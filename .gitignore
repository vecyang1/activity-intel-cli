# Baidu Netdisk sync artifacts. Transient, per-file, and they appear anywhere in
# the tree while an upload is in flight — five of them were committed once.
.*.baiduyun.uploading.cfg
*.baiduyun.uploading.cfg

# Python
__pycache__/
*.py[cod]
.DS_Store
*.egg-info/
build/
dist/
.venv/
venv/

# Fetched listings are Klook's and Airbnb's, not ours (see NOTICE). Fixtures are
# the one deliberate exception and are tracked explicitly.
out/
*.catalog.json
*.compare.json
